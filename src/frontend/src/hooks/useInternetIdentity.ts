// Re-export wrapper so existing imports of '../hooks/useInternetIdentity' continue to work.
export {
  useInternetIdentity,
  InternetIdentityProvider,
  type InternetIdentityContext,
  type Status,
} from "@caffeineai/core-infrastructure";
